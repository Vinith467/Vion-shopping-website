const fs = require('fs'); let content = fs.readFileSync('frontend/src/screens/admin/AdminInventory.jsx', 'utf8'); const start1 = content.indexOf('{/* Craftsmanship Features Manager */}'); const end1 = content.indexOf('{/* SEO & Meta */}'); if (start1 !== -1 && end1 !== -1) { content = content.substring(0, start1) + content.substring(end1); } const start2 = content.indexOf('{/* Craftsmanship Features */}'); const end2 = content.indexOf('{/* Submit Button'); if (start2 !== -1 && end2 !== -1) { content = content.substring(0, start2) + content.substring(end2); } fs.writeFileSync('frontend/src/screens/admin/AdminInventory.jsx', content);

const fs = require('fs'); let content = fs.readFileSync('frontend/src/screens/ProductDetailScreen.jsx', 'utf8'); const start = content.indexOf('{/* Craftsmanship Section */}'); if (start !== -1) { const end = content.indexOf('</div>', start + 1000); if (end !== -1) { content = content.substring(0, start) + content.substring(content.lastIndexOf('</div>', content.length - 5)); } } fs.writeFileSync('frontend/src/screens/ProductDetailScreen.jsx', content);
